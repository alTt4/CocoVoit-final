{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3">
            You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
        </div>
    {% endif %}

    <h1 class="h3 mb-3 font-weight-normal">Se connecter</h1>
    <div class="row justify-content-around connecterform">
        <div class="col-12 col-sm-12 col-md-12 col-lg-12 col-xl-12">
            <label for="inputEmail" class="labelconnexion first">Email : </label>
            <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="inputconnexion" autocomplete="email" required>
        </div><br><br>
        <div class="col-12 col-sm-12 col-md-12 col-lg-12 col-xl-12">
            <label for="inputPassword" class="labelconnexion">Mot de passe : </label>
            <input type="password" name="password" id="inputPassword" class="inputconnexion" autocomplete="current-password" required>
        </div><br><br>
        <div class="col-12 col-sm-12 col-md-12 col-lg-12 col-xl-12">
        <a href="{{ path('oubliPass')}}">Mot de passe oublié ?</a>
        </div><br><br>
        <div class="col-12 col-sm-12 col-md-6 col-lg-6 col-xl-6">
            <button class="btn" type="submit">
                Valider
            </button>
        </div>
    <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}"
    >
    </div>
    {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
    #}
</form>
{% endblock %}

{# <h1>Se connecter</h1>
    <form>
        <div class="row justify-content-around connecterform">
            <div class="col-12 col-sm-12 col-md-12 col-lg-12 col-xl-12">
                <label for="email" class="labelconnexion first">Email : </label>
                <input type="text" class="inputconnexion">
            </div><br><br>
            <div class="col-12 col-sm-12 col-md-12 col-lg-12 col-xl-12">
                <label for="motdepasse" class="labelconnexion">Mot de passe : </label>
                <input type="text" class="inputconnexion">
            </div><br><br>
            <div class="col-12 col-sm-12 col-md-12 col-lg-12 col-xl-12">
                <a href="mdpoublie.php">Mot de passe oublié ?</a>
            </div><br><br>
            <!-- CAPTCHA A AJOUTER !!! -->
            <div class="col-12 col-sm-12 col-md-6 col-lg-6 col-xl-6">
                <input type="submit" value="Valider" class="submitbutton">
            </div>
        </div>
    </form>
    <br> #}